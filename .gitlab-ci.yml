image:
  name: docker/compose:1.23.1
  entrypoint: [""]

unit_test:
  services:
    - docker

  before_script:
    - cd /builds/gusisoft/OneLike/users-service

  script:
    - sudo docker-compose -f docker-compose-dev.yml up -d --build
    - sudo docker-compose -f docker-compose-dev.yml run users python manage.py recreate-db
    - sudo docker-compose -f docker-compose-dev.yml run users python manage.py test
    - sudo docker-compose -f docker-compose-dev.yml run users flake8 project
